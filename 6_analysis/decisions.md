# Decision log

| date | topic | decision | reason | affected files |
|---|---|---|---|---|
| 2026-08-27 | Evaluation metrics | Every reported metric must be defined mathematically, explained in plain language, and accompanied by its ideal value and main limitation. Relative L2 must be explicitly justified rather than introduced without explanation. | Readers must know what each number measures and why it is appropriate. | `section_blueprint.md`, future methodology text and figure captions |
| 2026-08-27 | Evaluation regions | Include a dedicated geometry figure showing the exact spatial extent and node count of every evaluation region. | Regional results are not interpretable unless readers can see where each region lies. | `figure_blueprint.md`, future region contract |
| 2026-08-27 | Force analysis | Remove force and drag/lift analysis from the current scope. Reconsider it only after the field and control-parameter analyses are complete. | Surface loads answer a different question and the existing force evidence is incomplete. | `analysis_matrix.csv`, `section_blueprint.md`, `figure_blueprint.md` |
| 2026-08-27 | Excluded experiments | Exclude Arms 2 and 3 because they use the fluctuation-inlet condition; exclude Arms 2 and 14 because BVF is out of scope; exclude Arm 16 from optimizer claims because several settings changed simultaneously. | These experiments do not match the agreed scope or do not isolate one controlled variable. | `analysis_matrix.csv`, future results tables |
| 2026-08-27 | Control parameters | Focus sensitivity analysis on measurement information, pressure-tap count, collocation strategy, Karman prior, prior-plus-collocation, and pressure noise. Do not analyse Adam versus no Adam. | These comparisons have defensible matched reference arms; the available Adam comparison is confounded. | `analysis_matrix.csv`, `section_blueprint.md` |
| 2026-08-27 | Terminology | Remove discussion of the fluctuation-inlet boundary condition. Retain the first shedding harmonic, especially v1, because it is the physical wake quantity targeted by the Karman prior. | The boundary-condition option and the physical shedding harmonic are different concepts. | future methodology, results, and captions |
| 2026-08-27 | A00 data basis | Use the canonical raw CFD file with the validated float32 cache; evaluate all 201 snapshots on the strict `(-4,8) x (-4,4)` crop. Treat far core as a nested subset of far wake, not as another partition region. | The cache agrees with the raw first snapshot within float32 round-off and the masks reproduce all stored regional node counts. | `data_contract.md`, `derived/source_inventory.csv`, Figure F0 |
| 2026-08-27 | Pressure gauge sensitivity | Computed the zero-mean sensitivity metric the contract allows. A best-fit constant offset explains part but not most of the far-field pressure error. Far-wake rel_L2: 2.33→0.81 (Arm 1), 1.57→1.43 (Arm 15). Far-core rel_L2: 1.90→0.72 (Arm 1), 1.28→1.15 (Arm 15). Arm 1 drops below 1.0 in both regions after correction; Arm 15 remains above 1.0 in both. Report both numbers; the raw result remains primary per the contract. | Confirms the pressure error is not purely a gauge artefact for Arm 15; for Arm 1 the offset explains most (not all) of the apparent far-field pressure deficit. | `derived/a04_pressure_gauge_check.json` |
| 2026-08-27 | Upstream ('other' region) v1 anomaly | Checked in absolute terms, not just amp_ratio. Arm 15's upstream leakage is real but small: RMS 0.0647 vs a physically-near-zero true RMS of 0.0056, and only 37% of the far-core signal magnitude. The 11.5x figure is a ratio-against-near-zero artefact, not a large absolute error. Decision: report as a caveat on 'other'/whole-domain regional summaries; do not treat as a defect requiring a code fix, since the trust region is deliberately inactive there (x<0, xstart=3.0) and a broader trust region would only suppress amplitude without adding real information. | Prevents a misleading "11x too much oscillation" headline while still being honest that the region is unconstrained upstream. | `derived/a04_v1_absolute_check.json` |
| 2026-08-27 | A01 dense reference status | **SUPERSEDED 2026-08-31.** The dense-observation run stopped on its function-evaluation cap (`STOP: TOTAL NO. of f AND g EVALUATIONS EXCEEDS LIMIT`, Tnf=40001 vs `--LBFGSMaxfun 40000`; the iteration cap was NOT reached, Tit=37924 vs `--LBFGSMaxit 40000`; `--SkipAdam` set). The original decision called it a lower bound. | Superseded because an optimizer stopping reason does not establish the direction in which reconstruction error would move under additional optimisation. | `findings.md` (A01), `figures/final/F03_information_comparison.png` caption; see the 2026-08-31 optimizer-stopping decision below |
| 2026-08-28 | Arm 01 (pressure-only + physics baseline) status | **SUPERSEDED 2026-08-31.** Terminates on SciPy's `REL_REDUCTION_OF_F` test after a failed line search (5,503 evaluations, loss 2.97e-4); a controlled matched-effort re-run under identical code/flags/seed also failed to converge — its first L-BFGS call died even earlier (2,727 evaluations), and cycling Adam kicks with fresh L-BFGS calls afterward reached 7,173 evaluations total (more than the original) but a worse final loss (5.25e-4, 1.77x higher). The original decision called it a lower bound. | Superseded because a lower training loss does not guarantee a lower reconstruction error, so the endpoint cannot be assigned a one-sided accuracy bound. | `findings.md` (A02, A04, "Training effort"), `notebooks/matched_effort/01b_matched_effort_outcome.md`, `notebooks/matched_effort/01b_matched_effort_comparison.csv`; see the 2026-08-31 optimizer-stopping decision below |
| 2026-08-28 | A03 acceptance | Accept A03 as `complete_with_caveat` rather than re-running it or dropping it from scope. Report the observed endpoints and the common result that none recovers the wake; do not assign the near-body gain or downstream loss causally to sampling. | The arms differ in one input flag but not in effort: 5,503 L-BFGS evaluations (uniform) against 43,676 and 37,713 (wake-biased), a 6.9-7.9x gap. Evaluation count is not an accuracy proxy, so it cannot provide a directional bound. | `analysis_matrix.csv`, `findings.md` (A03), `results_master.csv`, `scripts/a03_finalize_results.py`, `figures/final/F04b_collocation_strategy.png` |
| 2026-08-28 | Terminal blow-up definition | When the census scripts were recovered, two of them measured terminal gradient blow-up over different baseline windows while printing the same caption wording: an array-index window (6.5x for arm 01, 26.7x for the re-run) and an iteration-number window (5.7x, 25.5x). Adopt the array-index (census) definition everywhere and quote only those numbers; `05_two_run_divergence.py` keeps its own window but now prints a note not to quote it. | Two figures in the same chapter must not attach the same stated definition to different numbers. The choice is not material to the finding -- Spearman rho = -0.733 against -0.721 across the 17 runs -- so the tie is broken in favour of the definition the census and `findings.md` already used. | `scripts/termination_census/`, `findings.md` ("Training effort"), `figures/final/F_termination_anatomy.png`, `figures/final/F_arm1_rerun_breakdown.png` |
| 2026-08-28 | A03 upstream leakage | Report the absolute upstream numbers (predicted v1 RMS 0.0068 uniform against 0.0451 wake-biased, true 0.0056) rather than the 8.0x amplitude ratio, and treat the leakage as a caveat on `other`/whole-domain summaries rather than a defect. No code fix. | The ratio is inflated by a near-zero denominator, as with A04's 11.5x, but unlike A04 the absolute leakage genuinely rises 6.6x, so the ratio cannot simply be dismissed. The `--V1RadialTrust` explanation was tested and disproved: arm 15, which has the trust region, leaks most in absolute terms (0.0647), because the trust region acts from x >= 3 and is inactive upstream. Leakage tracks each arm's overall oscillation amplitude instead, which is the same amplitude-without-correctness behaviour A03 reports downstream. | `derived/a03_v1_absolute_check.json`, `scripts/a03_v1_absolute_check.py`, `findings.md` (A03) |
| 2026-08-31 | First-harmonic phase convention | Supersede all A01--A04 network $v_1$ relative-$L^2$ and phase values produced with the legacy window-time comparison. Fit the CFD harmonic in the numerically stable shifted coordinate, then rotate it into the network's absolute-time convention before comparison. Amplitude ratio and magnitude correlation are unchanged. | The former evaluator compared network modes referenced to absolute time against CFD coefficients referenced to $t-t_0$. A perfect reconstruction would therefore score about 0.289 instead of zero. The corrected evaluator now passes a non-zero-time-origin perfect-model test. Full time-domain field errors and the Gappy POD pairwise results were never affected. | `scripts/evaluate_common.py`, `scripts/verify_evaluate_common.py`, `derived/v1_phase_correction_audit.csv`, `derived/verified_results_audit.json`, `results_master.csv` |
| 2026-08-31 | Optimizer stopping language | Supersede the earlier "lower bound" wording for runs that stopped at an optimizer limit or early termination. Treat each as the observed endpoint of its recorded protocol, not a proven lower bound or optimum. | Additional optimisation may reduce the training objective without improving reconstruction error; directionality cannot be guaranteed from the stopping reason alone. | `findings.md`, dissertation Results and Discussion |
