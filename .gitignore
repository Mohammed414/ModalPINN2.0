/Data/
__pycache__/
.DS_Store
*.zip
PROJECT_LOG.md
PLAN_E3_pressure_only.md
R3 fluctuation inlet bc plan.md
R5 measured best candidates plan.md

# Large, regenerable gappy-POD intermediates (re-run build_pod_basis.py /
# gappy_reconstruct.py to recreate - pod_basis.npz is ~33MB,
# gappy_reconstructions.npz is ~1.2GB, neither belongs in git)
src/gappy_pod/pod_basis.npz
src/gappy_pod/gappy_reconstructions.npz

# Large, regenerable parsed-flow cache (re-run extras/parse_flow.py against
# data/fixed_cylinder_atRe100 to recreate - ~170MB, not source data)
extras/flow_cache.npz

# EnKF pressure-only project: large, regenerable, evaluation-only ground
# truth (re-run enkf_pressure_only/evaluation/build_reference_truth.py to
# recreate - reference_truth_full.npz is ~110MB raw scattered CFD truth,
# used ONLY by evaluation/ code, never by the estimator). tap_observations.npz
# and reference_truth_modal.npz are small and stay committed.
enkf_pressure_only/evaluation/_flow_cache.npz
enkf_pressure_only/data/reference_truth_full.npz

# EnKF pressure-only project: regenerable solver spin-up cache (re-run
# enkf_pressure_only/experiments/spin_up_solver.py to recreate - ~134MB,
# pure function of the validated Stage B solver config).
enkf_pressure_only/experiments/spinup_snapshots.npz
